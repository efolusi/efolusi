# Production landing-page routes. TLS uses the existing Cloudflare origin certificate.
server {
    listen 80;
    listen [::]:80;
    server_name efolusi.com www.efolusi.com;
    return 301 https://efolusi.com$request_uri;
}

server {
    listen 443 ssl;
    listen [::]:443 ssl;
    server_name www.efolusi.com;
    ssl_certificate /etc/nginx/ssl/efolusi.com.pem;
    ssl_certificate_key /etc/nginx/ssl/efolusi.com.key;
    ssl_protocols TLSv1.2 TLSv1.3;
    return 301 https://efolusi.com$request_uri;
}

server {
    listen 443 ssl;
    listen [::]:443 ssl;
    server_name efolusi.com;
    ssl_certificate /etc/nginx/ssl/efolusi.com.pem;
    ssl_certificate_key /etc/nginx/ssl/efolusi.com.key;
    ssl_protocols TLSv1.2 TLSv1.3;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}
